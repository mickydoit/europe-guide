# Plan 1 — execution notes (15 Sep 2026)

Ledger extract: rulings, parked findings and deferred minors from the subagent-driven run. Kept so Plan 2+ can triage them.

- | T1 workflow ↔ branch rule | deploy on main only | skill forbids implementing on main; Pages verification needs a deploy | Ruling: work on branch plan-1-foundation, workflow also triggers on that branch, merge to main at finish — costs one workflow-line cleanup if wrong |
- | T1 build script | `tsc -b` | no project references → tsc -b fails | Ruling: `tsc --noEmit && vite build`; add "node" to tsconfig types (scripts/ uses process) |
- | T5 options parent | lastStop | Lisbon snack table follows the whole Evening table, so lastStop = Mesa de Frades not the "pick one" row; Valle test asserts the pick row | Ruling: track lastPick (row matching /pick one|options? below|choose one/), parent = lastPick ?? lastStop — costs a wrong parent on an unusual file |
- | T6 book_by/decide_by | text → date column | Lisbon has `2026-09-15 (immediately)` → date insert fails | Ruling: leading ISO date to column, remainder to fields.<k>_note |
- | T11 import_city SQL | plpgsql var `slug` vs column `slug` | ambiguous reference error | Ruling: rename to v_slug |
- Task 1: Ruling: amend trailer to the plan's mandated text and force-push plan-1-foundation (branch has no other consumers) — costs nothing if wrong.
- Task 1: minor (deferred): tsconfig.json adds "vitest/globals" to the shared app types (test tooling leaks into app type scope)
- Task 1: minor (deferred): package.json keeps npm-init boilerplate (main, directories, keywords, author, license ISC, bugs, homepage)
- Task 1: minor (deferred): EBADENGINE warnings for jsdom@30/vitest@5 on Node 25 locally (CI on Node 22 unaffected)
- Task 1: Ruling: controller amended the trailer itself (message-only change, no code, no review needed) and force-pushed — costs nothing if wrong
- Task 2: Ruling: Supabase region eu-central-1 (Frankfurt) instead of the plan's eu-west-2 — central to Lisbon/Istanbul/Spain for trip latency — costs nothing if wrong. Owner-user creation (step 5 curl) is done by the controller once the owner supplies the password; implementer does everything else.
- Task 2: minor (deferred): config.toml [auth.email] enable_signup still true beside top-level enable_signup=false — set false for clarity
- Task 2: minor (deferred): state tables intentionally have no FK to content (survive re-import) — add a comment in the migration
- Task 2: minor (deferred): Row types omit `owner`; Task 11 stampOwner injects it — already planned
- Task 2: minor (deferred): config push also synced unrelated defaults (mfa/otp/storage analytics); harmless
- Task 2: minor (deferred): RLS predicates use bare auth.uid() rather than (select auth.uid()); irrelevant at this scale
- Task 3: minor (deferred): More.tsx drops the `error` half of the trips fetch (plan-mandated stub) — surface errors when More is built out in Plan 2
- Task 3: minor (deferred): smoke test carries an unused `from()` mock
- Task 4: Ruling: fix both — normalise `\r\n`→`\n` at the top of parseFrontMatter and tokenize, add a tokenize throw test with a broken fixture — cheap, owner's md files may come from Windows/pasted sources; costs one fix round. Fix round deferred until Task 6 implementer finishes (single-implementer rule).
- Task 4: minor (deferred): heading regex can't match 4+ `#`; H4 falls through as paragraph text
- Task 4: minor (deferred): no test for a heading/paragraph immediately after a table without a blank line
- Task 4: minor (deferred): inconsistent trimming between front-matter and tokenizer paths
- Task 6: minor (deferred): row-level errors report the table/bullets node line, not the offending row (tokenizer Node has no per-row lines) — shared with itinerary parser
- Task 6: minor (deferred): COLS set contains keys always intercepted earlier (dead entries); add a comment
- Task 5: Ruling: (1) extractPlace misses `**Name** trailing text` (Lisbon has `**Portas do Sol / Graça** golden hour`) — extend the regex in Task 8 so a bold name followed by whitespace+text yields place_name with null address; (2) options table accepts any header starting "Place" — require header[1] === 'address' in Task 8; (3) trip-title H1 bypass accepts any non-day H1 — require it to contain a comma and a 4-digit year, else fail as unrecognised heading, in Task 8. Folded into Task 8 because it moves/edits this code anyway — costs one larger Task 8 review if wrong.
- Task 5: parked — extractPlace exclusion list is a hardcoded English phrase list — Ruling: acceptable for a single owner's files; false places only cost a cached geocode and geocode misses are reported; revisit if a city import shows junk markers.
- Task 5: minor (deferred): row-level time errors report the table's first line (Node has no per-row lines)
- Task 5: minor (deferred): unreachable `if (!day)` guard inside push()
- Task 5: minor (deferred): duplicate-id suffixing untested
- Task 5: complete (commits 257ac95..94d0941, review clean with 3 rulings carried to Task 8, 1 parked, 3 deferred minors)
- Task 7: implemented d68567f. Review: Approved, 0 Important. Lisbon: 12 routes (W1..S3), F1 driving, 25 parked venues.
- Task 7: minor (deferred): no regression tests for route-after-totals-table or missing-url-caught-at-next-heading (both verified correct by reviewer)
- Task 7: minor (deferred): paragraphs before the first day heading in the routes file are silently dropped
- Task 7: minor (deferred): missing-url error points at the next heading's line
- Task 8: minor (deferred): address capture leaks a following `**bold**` span (e.g. `, Lisboa **BOOKED**`) — make `**` a cut boundary
- Task 8: minor (deferred): pointsFromGoogleUrl turns a percent-encoded literal `+` (%2B) into a space
- Task 8: minor (deferred): `%7C` in a waypoint name splits as a delimiter (matches Google's convention; acceptable)
- Task 9: Ruling: fix now in a fix round before Task 11 runs the real import — check res.ok, include status + body snippet + query in the thrown error — cheap, costs one round.
- Task 9: minor (deferred): loadEnv() validation untested
- Task 9: minor (deferred): repeated geocode misses re-hit Google (nulls not cached; accepted in plan)
- Task 10: Ruling: fix — readFile must throw with the path; buildOfflineAreas must fail if the extracted file is missing or empty — costs one fix round; queued behind the Task 11 implementer (single-implementer rule).
- Task 10: minor (deferred): clusterPoints generalised to <T extends P> beyond the brief's signature (harmless)
- Task 10: minor (deferred): --bbox uses default Number.toString (fine at these magnitudes)
- Task 11: Ruling: fix round covers Important #1 plus minors #2 (explicit grant execute to service_role), #3 (search_path = public, pg_temp), #4 (README: dry run writes geocode_cache and calls Routes), #5 (reject unknown flags) — all one-liners that de-risk the first real run.
- Task 11: minor (deferred): --maxzoom without value → NaN; duplicate-suffix files resolved silently; route_link with no matching route is silently null; loadEnv demands all keys even for --dry-run; tripMeta throws plain Error; items insert ORDER BY is a no-op (harmless); assembleCity regexes dates: from raw source; printReport prints Open: URL on dry run
- Task 11: NOTE for Plan 2: real Lisbon has 12 routes but only 1 route_link item (other route links live inside Details cells as **[Route](…)**) — the Day screen should link routes by date/block from the routes table, not rely on route_link items.
- Final: Ruling: ONE fix wave covering #1,#2,#4,#5,#6,#7,#8,L28 + cheap minors (bad-time test, auth getSession catch, supabase empty-string guard, min password 8, route_link no-match warning); then one scoped re-review; then merge to main. #1 lands in the wave and main becomes the only deploy branch at merge.
- Final: INCIDENT — fix item 2 (`[auth.email] enable_signup = false`) disabled the email PROVIDER; owner saw "Email logins are disabled". Controller reverted to true and pushed config. Ruling: the final reviewer's finding #2 / deferred L7 was wrong — `[auth.email] enable_signup` = email provider on/off; sign-ups are gated by top-level `[auth] enable_signup = false` only. Keep it true permanently. Costs nothing further.
