# Plan 3 — execution notes (15 Sep 2026)

Ledger extract for Plan 4 triage.

- | Pair / task | Finding | Ruling |
- | T2 caches API in jsdom | jsdom has no `caches` | Ruling: tests install a minimal in-memory CacheStorage polyfill on globalThis in the test file; production uses the real one |
- | T3 multiple sources | layers() per source → duplicate layer ids | Ruling: suffix every layer id with the source id |
- Task 1: minor (deferred): Placeholder.tsx now unreferenced; haversine test tolerance differs from brief wording (implementation correct)
- Task 2: minor (deferred): content-length branch of cachedMapStatus untested (Node never sets it); FakeCache duplicated across two test files
- Task 3: minor (deferred): parked/places carry an extra `kind` prop; unresolved route_id legs skipped silently; blunt ** strip; weak all-dates test
- Task 4: Ruling: fix round 1 = C1, I2, I3, I4 + minors 8 (log errors), 9 (clear tileError on next successful tile/sourcedata), 13 (['boolean',…]), 14 (add json to globPatterns), 12 (mounted guard), 6 (locate triggers getCurrentPosition when no fix), 5 (text-allow-overlap + white text on done). Then re-review, then Task 5.
- Task 6: minor (deferred): getCurrent/mapCurrent unused until Home exists (Plan 4); report test-count arithmetic
- Ruling: Task 4 round 2 runs while Task 5 edits Map.tsx/MapSheet.tsx/places.ts — disjoint files, both told never to stash; costs a rebase if they collide.
- Task 5: review Approved, 0 Important. minor (deferred): no regression tests for single-fetch-per-burst and toggle-off cancellation; tourist_attraction included in unrated keep list
- All 6 coding tasks complete. Final whole-branch review dispatched on 00dd3e5..ac7b0d0.
- Final review (opus): Ready With Fixes. C1 resolveBasemap has no error path (blank map, no banner). I1 signed URLs expire after 1 h with no re-sign. I2 weather icons break offline (no onError fallback). I3 saved places have no read surface. I4 offline writes surface raw errors. I5 8 sources × 70 layers ≈ 560 layers + seams (plan-level → single merged extract in Plan 4). I6 12 MB re-read on every map mount. I7 no navigator.storage.persist(). I8 Places cost unbounded except by gating (owner sets quota; ~$87 worst case within trial). I9 Day→Map link drops the date. Minors M1 tap targets 18 px … M12.
- Final: Ruling: ONE fix wave before merge: C1, I1 (re-resolve on tile error in signed mode), I2, I4 (offline message), I6 (module cache of PMTiles by key), I7 (persist() at startup + re-download hint), I9 (/map/:date), M1 (invisible hit layers r 20), M2 (guard date ''), M4 (glyph range 8192-8447), M5 (log status not URL), M10 (Close → /day when no in-app history via location.key), plus a minimal read surface for I3: "Saved nearby" chips under the Day weather strip from useDayNotes.savedPlaces with Walk-there + remove. Deferred to Plan 4: I5 merged extract, I8 photo mask, M3, M6–M9, M11, M12. Weather window: forecastOpensOn is correct (20 Sep for 29 Sep); the plan checklist's "19 Sep" was wrong.
- Final fix wave ecc3024: 13/13 addressed; re-review found 1 Important regression (pmtilesRegistry never invalidated after a re-download → stale tiles until reload) + 1 minor (saved chips flash across day change).
- Ruling: one targeted fix (deviation from "no second wave") because re-imports during the trip will hit it: a `mapsGeneration` counter in offlineMaps.ts bumped by downloadCityMaps/deleteCityMaps; Map.tsx clears its registry when the generation changes. Chip flash: gate on !loading and reset on date change.

## Carried to Plan 4 (from the final review)
- I5: one merged PMTiles extract per city at import (8 sources × ~70 layers ≈ 560 layers; seams where areas overlap). Data-pipeline change; cacheKey/MemorySource unchanged.
- I8: drop places.photos from the Nearby field mask; fetch a photo only when a sheet opens. Owner sets the daily quota cap in Google Cloud.
- Write queue wrapping state.ts (toggle/save/upload/savePlace); cache attachment PDFs offline; Home screen (Figma) consuming getCurrent weather; delete Placeholder.tsx; M3 metadata record for cachedMapStatus; M6 BASE_URL for glyph/sprite paths; M7 leg mode styling; M8 All-mode numbering density; M9 camera loss on basemap swap.
- Phone checklist still open: Map renders real tiles + labels; airplane-mode parity after Download; seams between areas; pinch-zoom frame rate; tap accuracy; weather strip from 20 Sep for 29 Sep; Places '!' after ~150 m of movement.
