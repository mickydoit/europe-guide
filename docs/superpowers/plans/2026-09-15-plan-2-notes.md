# Plan 2 — execution notes (15 Sep 2026)

Ledger extract: rulings, deferred minors, final-review notes. Plan 3 (Map) and Plan 4 (Home/Places/write queue) should triage these.

- | Pair / task | Finding | Ruling |
- | T1 data.test | `vi.mock` factory references a top-level `mock` const → hoisting breaks it (seen in Plan 1 T3) | Ruling: tests call `fetchCityWith(client, slug)` / `fetchTripsWith(client)` directly; no vi.mock of the supabase module in data tests |
- | T2 ↔ T6/7/8 | `useTrip` shape | identical across tasks; T2 must export `TripProvider` and a `TripContext` so app tests can render with a fixed `content` without network (add `TripProvider value?` prop for tests) — Ruling: T2 adds an optional `initial?: { trips, slug, content }` prop used only by tests |
- | T5 useAttachments | needs ownerId from useAuth → tests must wrap AuthProvider mock | Ruling: `useAttachments` takes `ownerId` as an argument; screens pass `session.user.id` |
- Task 1: minor (deferred): thin real-client wrappers untested; mock order() is a no-op; "trip not found" error lacks table name
- Ruling: Tasks 2 (trip.tsx, App.tsx, smoke test) and 3 (time.ts + its test) run in parallel — disjoint files, both told to pull --rebase before push — to buy back schedule; costs a rebase if they collide on package files.
- Ruling revised: no more parallel implementers in the shared checkout (Task 3 had to stash Task 2's WIP) — serial from here, or a worktree if ever parallel.
- Task 3: minor (deferred): redundant `~` branch in fmtTime; fmtCountdown assumes integers
- Task 4: implemented ed750fe. Review: 2 Important. Ruling: (a) TOKEN_RE `[^)\s]+` stands — it was the controller's dispatch text and URLs with spaces are invalid; no fix. (b) href protocol guard — fix round 1 (allow only http(s):, mailto:, tel:; otherwise render as text).
- Task 4: minor (deferred): no tests for parentheses inside URLs or adjacent tokens
- Task 2: implemented c30bed0. Review: 1 Critical (city-load effect has no staleness guard — rapid setSlug can show trip A's content under trip B), 2 Important (no test for `initial` prop; no test for trips-fetch-failure with empty cache). Ruling: fix round 1 after Task 5 lands (serial rule); also use todayInTrip from time.ts instead of the duplicate todayIn helper.
- Task 2: minor (deferred): no dedicated test that useTrip throws outside the provider
- Task 5: implemented 9bdb9bc. Review: Approved; 2 Important follow-ups (toggle decision reads closure `done` → concurrent same-item toggles misrevert; fake insert doesn't synthesize `id` so remove() test is weak). Ruling: batch with Task 4's href guard into ONE fix dispatch after Task 2's fix lands: toggle decides from a ref mirror of the Set; upload validation also sets `error`; fake table synthesises an id on insert.
- Task 5: minor (deferred): no compensating storage delete if the attachments insert fails; vacuous waitFor in booking-state test; cost typing relies on TS
- Task 6: minor (deferred): useChecks fires with '' slug during loading (wasted query); RouteStrip dropped if a day has routes but no items; route_link text not through Md; TripPicker renders with one trip
- Task 7: implemented edce40e. Review: Approved; 3 Important follow-ups. Ruling: fold into the Task 9 dispatch — (a) effective status for kind==='booked' defaults to 'booked' when no state/file status; (b) Sheet locks body scroll while open; (c) tel: href keeps only digits and a leading '+'.
- Task 7: minor (deferred): blank currency saves null instead of EUR; parser-internal fields (book_by_note/for_note) would render humanised; walk-ins rendered as one joined string
- Task 8: minor (deferred): Refresh button not disabled while loading; Undated routes sort first; 0 m / 0 min rendered for zero metrics
- Task 9: minor (deferred): stripMd collapses newlines so DESCRIPTION is single-line (escapeIcs newline branch unreachable in practice); booked-status never-set vs cleared ambiguity
- All 9 coding tasks complete (Task 10 = deploy + phone checklist after merge). Final whole-branch review dispatched on 69c398f..da54c19.
- Final review (opus): Ready With Fixes. C1 Postgres `time` → HH:MM:SS breaks ics DTSTART and shows seconds in UI (normalise at data.ts boundary). I1 null-block items render after Evening (order groups by min sort). I2 tick/attachment errors unhandled. I3 window.open after await popup-blocked on iOS. I4 empty-trips check precedes error; stale slug has no way back. I5 NowNext never ticks. I6 28px tick target. I7 downloadIcs unverified in standalone PWA (phone checklist). I8 jwt_expiry 3600 could lock out offline (raise to 604800 + verify cold airplane launch).
- Final: Ruling: ONE fix wave: C1, I1–I6, I8 (config), plus cheap minors (fmtTime '—' in Bookings meta, priority normalise, parked key seq, dayIndex guard on /day/:date, X-WR-CALNAME escape, Sheet backdrop aria-hidden, `/` → redirect to /day until Plan 4 builds Home). Then one scoped re-review, then merge. I7 goes on the phone checklist.

## Final-review recommendations carried forward
- Plan 3: code-split the map route; legs already carry polyline + endpoints; reuse walkLink in the map sheet; maps bucket needs signed URLs/auth for PMTiles range requests.
- Plan 4: write queue wraps state.ts (toggle/save/upload); cache attachment PDFs for offline; build Home (/ currently redirects to /day); offline indicator on Day/Bookings; RequireAuth vs stale JWT offline — verify a cold airplane-mode launch >1 h after last online use.
- Phone checklist still open (needs Lisbon data): ticks persist; Walk there opens Google Maps; upload + reopen a PDF (window.open flow); calendar export from the Home-Screen PWA (blob download is unverified on iOS standalone); airplane-mode reads.
