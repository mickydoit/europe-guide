# Plan 4 — execution notes (15 Sep 2026)

Ledger extract: rulings, deferred minors, final-review residuals.

- | Pair / task | Finding | Ruling |
- Task 1: Ruling: fix round 1 on sync.ts (+tests) only, in parallel with Task 2 (state.ts/screens) — disjoint files; costs a rebase if collide.
- Task 2: Ruling: fix round 1 = C1, C2, I3, I4 + minors 5 (clear uploadQueued after 2 s), 6 (functional setState in booking save revert), 8 (offline-first remove for real attachment rows → enqueue an `attachment_delete`? NO — keep scope: show friendly offline error only), 9 (removePlace queued copy), 10 (single QUEUED_COPY constant). Tests must include a client whose select errors.
- Task 6: implemented fe449a3. Review: Approved, 0 Important. minor (deferred): no tests for null-date reminders or lng < LON0; heading "Snack options" (3-word cap) is terse — Home task may loosen to 4 words.
- Task 2: fix round 1/5 (4 addressed + 4 minors, 0 open; commit c59d90f). Re-review notes: useAttachments load-race IS reachable (upload button not gated on loading) — pre-existing; carry into Task 5 (which touches useAttachments); triple import in Bookings.tsx.
- Task 7: implemented 25805c6. Review: Approved, 0 Important. Controller rendered the branch build headlessly with real Lisbon data: header + "Plans for 29 Sep" caption, Portugal pill, world hero with the Lisbon dot correctly on Portugal, Open map, empty Options/Tours/Walks for the arrival day, Lisbon "In 14 days" tile, reminder chips ("Book Ponto Final — dinner by 15 Sep"). Weather overlay absent only because the owner's Google key rows still lack `*` (403).
- Task 7: minor (deferred): initials() on quoted names; visual polish is the owner's Figma pass
- Task 5: implemented 5f85c61. Review: Approved, 0 Important. minor (deferred): background cache-warm markCached lacks an unmount guard (harmless in React 18)
- Task 8: implemented f247c7e. Review: Approved. minor (deferred): refresh() in trip.tsx doesn't set loading=true so "Refreshing…" won't show from the button — candidate for the final fix wave.
- Final review (opus): Ready With Fixes. C1 settle() deletes NEWER queued ops (no time compare). I1 network-class replay failures burn attempts → queue parks in ~68 min on captive wifi. I2 attachments has no unique index on storage_path → replay can duplicate rows. I3 tickets cached only for bookings opened online. I4 200 MB cap enforced per booking. I5 old per-area cache files orphaned; stale seq-0 served as "1 of 1". Minors: no flush on start; full getAll in count/update; day_notes updated_at from createdAt; Home useBookingState('') query; Sync now no feedback/uncaught; toursToday zero duration; refresh() loading.
- Final: Ruling: ONE fix wave: C1, I1, I2 (migration 0006 unique index + dedupe + db push), I3 (warm pass: More/Home online → prefetch all trip attachments once per session), I4 (global cap across the trip's rows), I5 (purge unknown /__maps/<trip>/ keys; staleness when cached bytes ≠ size_bytes → status shows "Update needed"), + minors (flush on start, count()/get(id), Home guard, Sync now feedback + catch, toursToday clamp, refresh() sets loading). Then re-review, merge.
- Final fix wave 51b4389: all findings addressed (opus re-review). Residuals: blanket TypeError + break can wedge the queue on a malformed op (no parking, no UI); status-class branch unreachable from flushOutbox (replay rethrows bare Error); stale-map prompt gated by the old "Later" dismissal; warm memo never invalidated; syncMsg sticky; refresh() finally can clear a newer load's flag; cached set not corrected after eviction.
- Ruling: one targeted round before merge (deviation from "no second wave", justified by queue-wedge risk on a travel day): narrow TypeError classification to transport messages; cap consecutive network holds (60 → park with lastError "offline too long"); replay preserves code/status on rethrow; separate localStorage key for the stale prompt; invalidate the warm memo after a successful upload; clear syncMsg on `online`. Other residuals deferred to notes.

## Residuals deferred (from the final re-reviews)
- settle(): createdAt is enqueue time, not intent time — a double-tap on one item where the first write fails network-class after the second started can replay the older intent; stamp ops with the write's startedAt to close.
- useAttachments.cached not corrected after the warm pass evicts; warm caption stale until remount after uploads (resetWarm helps on next mount).
- refresh() finally can clear a newer load's loading flag (cosmetic).
- Map.test.tsx carries its own cache fake; collapse onto tests/helpers/fakeCaches.ts.
- A stale cached map is still served as basemap until updated (intended; undocumented at call site).

## Owner phone checklist for Plan 4
1. Airplane mode: open a booking's cached ticket PDF (blob URL into a new tab is the fragile iOS path).
2. More → Offline map: Delete, then Download; confirm '28.2 of 28.2 MB'; pan Lisbon offline — no gaps.
3. Open every booking with a ticket while online (or rely on the warm pass) — More shows 'Tickets saved for offline: N of M'.
4. Airplane mode: tick a stop, save a place from the map, upload a PDF → badge counts; force-quit + relaunch → still queued; reconnect → badge clears; reload → nothing reverts.
5. Home: dots on Lisbon; weather shows once the Google key rows end in /*.
6. Open the app online within 7 days of flying and again just before departure (session lifetime).
