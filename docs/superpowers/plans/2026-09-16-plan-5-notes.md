# Plan 5 (Figma redesign) — Task 9 notes

Branch `feat/figma-redesign`, base `0407b7b` (end of Plan 4). Final test count: **493 passed (54 files)**, `npx vitest run` clean; `npm run build` clean (`tsc --noEmit && vite build`).

## Per-task commits

`git log --oneline 0407b7b..HEAD` (oldest first):

| Task | Commits | Subject |
| --- | --- | --- |
| pre-flight | `543ddb1` | docs(plan 5): correct fixture-dependent test assertions (pre-flight rulings) |
| 1 — theme | `f385c02` | feat(theme): Figma palette, self-hosted Overpass/Poppins, nav and kind icons |
| 2 — tab bar | `18a7999` | feat(nav): five-tab bar with Figma icons, Tickets tab |
| 3 — pure functions | `35641bf`, `cdef08a`, `5d2135f` | feat(tickets): kind inference, next-up, grouping, card lines, status helpers; docs: fixture dates for Task 5/8 tests, date-aware `bookingForStop`; fix(tickets): `bookingForStop` prefers same-dated booking among name matches |
| 4 — cards | `2f1d25b`, `ba7130d`, `a5f1c00` | feat(cards): TicketCard, PlaceCard, NextUpCard, StatusPill from the Figma cards; docs: Task 6 test locates the status button beside the card link; fix(cards): stop status-pill CSS collision, split TicketCard link from status button |
| 5 — Home | `91f1da5`, `aea5eab` | feat(home): greeting, weather row, next-up ticket, ticket and place card rows; fix(home): restore coverage for reminders/warm-pass, add status-cycle and hero weather tests, clean up dead world-hero CSS |
| 6 — Tickets | `15da75c`, `46f7382` | feat(tickets): upcoming view by country, city and day; /bookings redirects; docs(plan 5): Task 8 carries review findings from Tasks 4–6 |
| 7 — detail screens | `0a94942`, `40b7920` | feat(detail): boarding-pass ticket and place detail screens; Bookings retired; fix(detail): recover BookingForm/AttachmentsPanel coverage, page heading, back nav, routeEnds separators |
| 8 — Day/Map/More/auth | `19b59a3`, `99f732e` | feat(restyle): Day rows with ticket links, More cards, Map sheet ticket link, auth theme; drop legacy tones and dead components; fix(restyle): prevent nested anchors in StopRow/PlaceCard, drop unused More locals, restore 44px tick target, retint Map lavender/highlight |
| 9 — this task | (this commit) | chore: headless screenshot tools and Plan 5 notes |

## Tooling built

- `tools/probe-session.mjs` — signs in as the owner via `supabase.auth.admin.generateLink` + `verifyOtp` (service key from `.env`, never printed) and writes the `access_token`/`refresh_token` fragment to a file outside the repo.
- `tools/probe-screens.mjs` — headless Chromium (Playwright's `chromium-1228`, `--use-angle=metal --enable-gpu` — required on this Mac or WebGL/Map fails), 390×844 @2x mobile viewport, consumes the session fragment via `/reset#…`, then screenshots Home, Day, Tickets, More, Map, the first ticket, and the first place.

### Deviations from the brief's script text

1. **`a.ticket-card` → `a.ticket-card__link`.** The brief's selector for the first ticket card link (`page.locator('a.ticket-card').first()`) never matches: `TicketCard` renders `<article class="ticket-card ticket-card--{kind}">` (not a link) containing `<a class="ticket-card__link">`. Fixed to `a.ticket-card__link` so the ticket/place screenshots are actually produced instead of silently skipped.
2. **Ticket/place deep links carry `?trip=`.** `/ticket/:trip/:id` and `/place/:id` read their data from the app-wide trip context (`useTrip()`), not from the `:trip` path segment. On a genuinely fresh full-page load (exactly what `page.goto` does) with no `?trip=` in the query string, `resolveSlug()` falls back to `defaultSlug()` (today's trip, else `trips[0]`) rather than the trip the link came from. Confirmed this is a real gap, not just a probe artifact — see Finding 1 below. Fixed the probe by appending `?trip=${trip}` to the extracted `href` before navigating, so the ticket/place screenshots reflect the intended city.

Both fixes are one-line changes inside `tools/probe-screens.mjs` only; no application code was touched for either.

## Findings for the controller

1. **Deep-link trip context (app behavior, not fixed here).** `TicketDetail`/`PlaceDetail` resolve their data from `useTrip()`'s ambient trip context, not from the `:trip` path segment in their own route. Client-side (in-app) navigation is unaffected because the trip context is already loaded and persists across route changes. But a cold full-page load of `/ticket/<trip>/<id>` or `/place/<id>` with no `?trip=` in the URL (e.g. a bookmarked/shared link, or a hard refresh while on that screen) silently resolves the *wrong* trip — today it would 404 into whichever trip is chronologically first (`trips[0]`) or matches "today" — and shows "No ticket … in <wrong city>." Reproduced directly: `GET /ticket/seville/T04` cold gave "No ticket T04 in Lisbon." This is worth a real fix (e.g. have the route call `setSlug(tripParam)` on mount when it differs) but is out of scope for Task 9's screenshot tooling.
2. **Lisbon map over-zooms.** `/map/2026-10-01?trip=lisbon` renders zoomed out to show all of Portugal north of Lisbon (see `lisbon-map.png`, pin "11" up near Turcifal) instead of a tight fit on the day's stops in central Lisbon/Belém. One imported stop almost certainly has bad or missing lat/lng, dragging the auto-fit bounds out. Likely a `content/lisbon` import data problem, not a code bug — flagging for the controller/import step, not fixed here.
3. **First-ticket auto-pick is event-kind in both cities.** The brief's "first ticket card" pick (`Tickets` screen, first `<a class="ticket-card__link">`) happens to be a restaurant/event booking in both Seville (`La Chunga`) and Lisbon (`Mesa de Frades`), so `seville-ticket.png`/the Lisbon ticket render the **event-detail hero layout**, not the three-part boarding pass. Manually verified the boarding-pass layout separately against a real transport booking (`GET /ticket/seville/T04`, the AVE Seville→Barcelona train) — route ends, glyph, dotted mid-section, booking/ref cells, priority, and stub with status all render correctly. Included that extra screenshot as `seville-ticket-pass.png` so the owner can see both ticket layouts.
4. **`fullPage: true` screenshots interleave the fixed tab bar mid-page.** Because the tab bar is `position: fixed`, a full-page screenshot (used for Home/Day/Tickets/More/ticket/place) captures it wherever the viewport happened to be when the screenshot API stitches the page, so it appears to overlap content partway down several screenshots (e.g. `lisbon-home.png`'s Reminders section). This is a Playwright full-page-screenshot artifact, not a rendering bug in the live app — noting it so the owner isn't alarmed by an apparent overlap.

## Screenshot checklist results

Ran against both real imported cities: Seville (`2026-10-05`) and Lisbon (`2026-10-01`). All 14 raw screenshots taken; 12 selected into `docs/superpowers/plans/2026-09-16-plan-5-shots/` (kept both cities' Home/Tickets/Place/Map for comparison, one city's Day/More since those don't change shape between cities, plus the extra boarding-pass verification shot).

| Check | Result |
| --- | --- |
| Fonts are Overpass/Poppins, not system | **Pass.** Verified two ways: visually (distinct bold geometric headings, humanist body text) and programmatically — `document.fonts.check('16px Overpass')` → `true`; both `Poppins` faces (500/700) report `status: "loaded"` in `document.fonts`; `getComputedStyle(document.body).fontFamily` starts with `Overpass`. |
| Tab bar has five icons, yellow active one | **Pass.** Home/Day/Map/Tickets/More all present and in that order in every screenshot; the active tab's icon+underline is yellow, the rest are muted grey (see any of `seville-home.png`, `seville-day.png`, etc.). |
| Home shows greeting → weather → map → next-up → rows | **Pass.** `seville-home.png`/`lisbon-home.png`: "Hello… Michael" greeting, "Forecast opens on …" weather row, world map with a highlighted dot and "Open map" pill, a "Next up" ticket card, then Tickets/Tours-and-events card rows, then Reminders. |
| Tickets shows country tabs and day headers | **Pass.** `seville-tickets.png` shows Portugal/Spain tabs (Spain active) and per-day headings ("Sunday 4 October", "Monday 5 October", …); `lisbon-tickets.png` shows the same with Portugal active. City chips only appear when a country has more than one loaded city, which isn't the case here (one city loaded per country) — expected per the Task 6 spec amendment, not a bug. |
| Ticket screen shows the three-part pass | **Pass, with a caveat.** The auto-picked first ticket in both cities is event-kind, so `seville-ticket.png` shows the event-detail hero layout instead. Boarding-pass layout (route ends / glyph / dotted mid / booking-ref cells / stub with status) verified directly against a transport ticket and captured as `seville-ticket-pass.png` — see Finding 3. |
| Place screen shows the lavender hero | **Pass.** `seville-place.png` (Muy Río Coffee) and `lisbon-place.png` (Leave Benfica, CP train to Rossio) both show the `place-detail__hero` in the lavender surface with the kind glyph and title. |

No CSS/class-level visual defects were found that needed a one-line fix; the two script bugs found (selector, missing `?trip=`) were fixed in the tooling itself as noted above.

## Final fix wave (16 Sep 2026)

Whole-branch review findings, all fixed in one code commit (`0449a8f`) plus this screenshot
refresh. `npx vitest run` → **505 passed (54 files)** (was 493); `npm run build` clean; the Step 9
acceptance grep (`salmon|banana|granny|golden|columbia|coral|--card\b|--card-alt|--bg-2`) prints
nothing.

| # | What changed | Where |
| --- | --- | --- |
| C1 | `TicketDetail` waits for `useBookingState`'s `loading` before mounting `BookingForm`, and keys it `id:updated_at` so a later row remounts it. Save can no longer upsert nulls over a stored ref/cost/notes. | `src/screens/TicketDetail.tsx` |
| C2 | A cold `/ticket/:trip/:id` calls `setSlug(tripParam)` when the path names a known trip other than the ambient one, and shows Loading (not "No ticket") while the switch is in flight. Every generated `/ticket/…` and `/place/…` link now carries `?trip=`. | `TicketDetail.tsx`, `Home.tsx`, `Tickets.tsx`, `StopRow.tsx`, `PlaceDetail.tsx`, `Map.tsx` |
| I1 | `Md` renders `**[label](url)**` as `<strong><a>label</a></strong>` (or a bold label under `noLinks`) instead of printing brackets and the raw URL. | `src/components/Md.tsx` |
| I2 | `inferKind`/`kindIcon` recognise a flight code (`FR3628`, case-sensitive, digits required) and the common carriers, so `Ryanair FR3628 LIS → SVQ` is transport with the plane glyph. | `src/lib/tickets.ts` |
| I3 | `.pass__stub .field__input` gets a translucent fill — the form fields were invisible against `--surface`. | `src/styles/base.css` |
| I4 | `fourCells` skips generic `fields` values over 40 characters (Cost/Contact/Book by stay unconditional). | `src/lib/tickets.ts` |
| I5 | `.pass__status .status-pill { align-self: flex-start }` — the pill no longer stretches the column width. | `base.css` |
| I6 | Home switches to the trip whose dates contain today when the loaded one does not (once per mount, ref-guarded). | `src/screens/Home.tsx` |
| I7 | `PlaceDetail` mirrors Day: a queued tick shows `QUEUED_COPY`, a rejected one shows "Couldn't save — you may be offline", both clearing after 4 s. | `src/screens/PlaceDetail.tsx` |
| I8 | `button.status-pill` gets a 44 px hit area via `::before { inset: -12px -6px }`, visual size unchanged. | `base.css` |
| minor | Last three `#11da8f` accents retinted to `#22DD85` (Map `ACCENT`, focus ring, world-hero drop shadow); `ticketLines` event sub uses `b.fields.cost || b.address`; `.tickets-day__heading` sticks at `var(--safe-top)`; `tools/probe-session.mjs` takes the email from `PROBE_EMAIL`/argv[3] and exits with usage when neither is set. | various |

New tests: bold-link rendering both ways (`Md.test.tsx`), the Day `noLinks` test now asserts the
label survives without the URL, flight-code/carrier inference and the `FR Bistro` negative
(`tickets.test.ts`), the 40-char cell rule, the empty-cost event fallback, the deferred-then-landed
booking form and the cold cross-trip deep link (`TicketDetail.test.tsx`), the today-in-another-trip
switch and its no-op case (`Home.test.tsx`), queued and failed ticks (`PlaceDetail.test.tsx`).

`tools/probe-screens.mjs` no longer appends `?trip=` blindly — the app's own links carry it now, so
it only fills in when a link arrives without one.

### Screenshot refresh

All 12 existing PNGs in `2026-09-16-plan-5-shots/` re-captured against this build (Seville
2026-10-05, Lisbon 2026-10-01), plus a new `lisbon-ticket-flight.png` for the Ryanair booking
(`/ticket/lisbon/B04`). Read-through notes:

- `lisbon-ticket-flight.png` — **I2 confirmed live**: plane glyph, "Departs 07:55", the boarding-pass
  layout rather than the event hero. **I3/I5 confirmed**: Status/Confirmation ref/Cost/Notes fields
  are visible against the stub, and the "Booked" pill hugs its text.
- `seville-ticket-pass.png` (AVE Seville → Barcelona) — same, with the "Not booked" pill hugging and
  the single `Book by 17 Sep` cell; the `why_urgent`-style prose no longer appears in the strip (I4).
- `lisbon-tickets.png` — the Ryanair row is now a dark transport card with a plane, grouped under
  Sunday 4 October. `seville-tickets.png` unchanged in shape.
- Lisbon `day` — the route strip's "Open route" links render as links, not raw `**[…](…)**` (I1).
- `seville-home.png` — the "Tours and events" row still carries a card titled **FR3628 / Lands
  Seville 10:00 local**. That is a Seville *stop item* for the arriving flight; the matching booking
  lives in the Lisbon trip, so `bookingForStop` (single-trip by construction) cannot pair them. Not
  one of this wave's findings — flagging it as a cross-trip content/matching gap for the controller.
- `lisbon-map.png` — the over-zoom from the last wave's Finding 2 is unchanged (pin 11 still up near
  Turcifal). Still an import data problem, not a code one.
- The fixed tab bar still interleaves mid-page in `fullPage` shots — the Playwright artifact noted
  last wave, not a rendering bug.
- `seville-more.png` was re-captured with the account email replaced by `owner@example.com` in the
  DOM before the shot: this repo is public and the previous copy showed the owner's address. The
  older copy is still in git history at `632cb61` — worth a controller decision on whether that
  matters.
