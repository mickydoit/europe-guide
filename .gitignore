# Private trip data — never commit the real itinerary, tickets or bookings
content/
*.local.*
.env
.env.*
!.env.example
node_modules/
dist/
.DS_Store

# SDD scratch
.superpowers/
.cache/

# PWA / Supabase / map tiles
dev-dist/
supabase/.temp/
*.pmtiles
